# Stage 13 protocol, analysis, and production-manifest freeze

## Authority and boundary

Alex approved `stage13-package-a-protected-core-optional-five-task/v3` on
2026-08-31, verbatim and bound to decision-dossier SHA-256
`642118fe30e1c435fc05c656ac6167446c76a445c9a1efd376d5a7c23410b1f4`.
The additive approval record has SHA-256
`acd34de34ee94f943ddfb2d088572196d74c5bde9eae8474e691b5f8ef89d336`.
The approved dossier remains byte-for-byte unchanged with its internal approval
field pending; this preserves the dossier as pre-approval evidence rather than
rewriting history.

This freeze contains no scientific data, performs no teacher/student training,
discovery, exact evaluation, calibration, or Fourier intervention, and is not
production eligible. Stage 14 resource qualification and Stage 15 execution
have not started. No registered checkpoint, dense-output cache, private result,
credential, provider path, or unannounced resource was accessed or assumed.

## Resolutions

RD-001 through RD-012 are resolved exactly by the approved v3 recommendation
objects. RD-013 remains `pending_stage14`. RD-014 is resolved here only for the
scientific DAG, priorities, retries, retention, gates, and export policy; its
provider, scheduler, hardware, container, path, quota, credential, throughput,
and destination facts remain Stage 14 bindings.

- RD-001: Task 1 is modular addition mod 113, fixed teacher seeds 0-14,
  three frozen phases, no replacement, with the pre/stable matched pair primary.
  Task 2 modular multiplication mod 59 is protected. Tasks 3-5 are ordered,
  task-indexed optional increments and are not population replicates.
- RD-002: the four alternate architectures and canonical comparator use the
  same anchor seeds 0-4 and checkpoint panel. Only within-checkpoint paired,
  conditional external-validity contrasts are permitted.
- RD-003: hard and soft targets remain separate; all fixed attempts and failures
  are retained; exact hard agreement and centered-logit soft eligibility are
  applied without imputation or replacement.
- RD-004: canonical and four alternative bases use the shared seed-0-4
  direct/hard/soft checkpoint panel. Raw, parameter-weighted, and component-type
  denominators are retained.
- RD-005: centered-logit fidelity at 0.99 is primary; 0.90, 0.95, 0.975, 0.99,
  and 0.995 form the reducer-only frontier. The intact mask fixes the defined
  upper endpoint at 1.0.
- RD-006: Endpoint 1 is primary. Packing at component cap 0.25 and maximum
  Jaccard overlap 0.25 is a key-secondary, procedure-relative lower bound. The
  3-by-4 cap/overlap grid is reducer-only from sealed ledgers.
- RD-007: greedy deletion and hard-concrete gates retain distinct native budget
  units and use 256 total exact evaluations per model-method ledger, including
  the intact mask. Within-method interpretation is primary.
- RD-008/RD-009: combinatorial, ordinary-restart, local-perturbation, and exact
  tractable layers retain their coverage rules. The tractable universe is
  exactly `2^18 = 262144` masks and calibrates procedure behavior only.
- RD-010: teacher seed is the population unit. Student initializations describe
  conditional realization sensitivity. Paired pre/stable contrasts, raw seed
  rows, exact sign-flip tests, bootstrap intervals, missingness, multiplicity,
  and all six nondirectional outcome categories are fixed.
- RD-011: the intervention is the modular-sum code in final-token
  `blocks.0.mlp.hook_post`, before `W_out`. Four diagonal modular-addition modes
  are primary; the off-diagonal wrong-mode support is disjoint. The primary
  outcome is counterfactual centered-logit displacement fidelity. All five
  capacity-matched controls are mandatory, and uniqueness claims are prohibited.
- RD-012: launch scope is Tier 1 plus protected Tier 2. Optional Tasks 3, 4,
  and 5 may be admitted only in that order, one whole sealed increment at a
  time, after protected completion is secure.

## Frozen artifacts

| Artifact | SHA-256 |
|---|---|
| Frozen scientific protocol | `da7ed31537727f92a66f72319711786e0b60b51d2a08646a2010c779ecdeca9f` |
| Production profiles | `c5f96f38bdbfb1b42419eda0b22ffce10748d14be91b2fc2673e16412e893b6d` |
| Analysis/report plan | `4f12fef3b5a25ef271e2532da4e3208d447a5d3981a2a9884cf788ac1ae4d082` |
| Job-array specification | `701b8cac0daaccae94f0bd243c04c293fed320bdd39230bc1f49995d92c70a58` |
| Expanded-manifest seal file | `7f1a189ad62503faa5bd8f013f77afba65a803f0f15962f01b61effaa648214a` |
| Expanded canonical members | `adbfb30694bb984de4d8ba582cee0efb468b8f9a2fce01f6a3654b5b78b1927b` |
| Campaign root | `b369c35de0af04afc33f9ed1777fbf0db1542a32b0a3e0c830d25d361d6c66a6` |
| Excluded-evidence register | `ba57c9b2ad8636d727d787299f905bf5379cbf77d5314fe368a8307a0004cf43` |
| Resource projection | `7f43b4560f63bd81b63f20e97cc152dedc09fae412c600f4eafc699f80b06186` |
| Synthetic fixture | `e90c39d931d87985759aa1cba43854cd4e144765177496f821aa93459380a9` |
| Synthetic complete report | `d66b652f27cab03e3b90e1be0bf0f3557b9fa6244f96413c27c2bd54ad74a096` |

The schemas under `followup/schemas/stage13/` close every artifact root to
unknown fields. The integrated validator additionally binds every nested
approved recommendation back to the immutable dossier, so a nested change is
rejected even if it is syntactically valid JSON.

## Declarative campaign and accounting

The compact array specification expands to 8,745 logical jobs. It stores no
backend coordinates. Every member has a canonical logical identity, a
domain-separated seed, dependency-array identity seals, a frozen config hash,
resource class, priority, concurrency group, portable output, retention rule,
retry class, terminal states, and no-imputation consequence.

| Scope | Logical jobs | Teacher training | Maximum student attempts | Greedy ledgers | Hard-concrete ledgers/restarts | Exact evaluations | Fourier trials | Merge/export |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Protected core | 7,884 | 21 | 940 | 645 | 645 / 2,580 | 615,424 | 92,160 | 1 |
| Optional Task 3 | 287 | 5 | 40 | 30 | 30 / 120 | 15,360 | 0 | 1 |
| Optional Task 4 | 287 | 5 | 40 | 30 | 30 / 120 | 15,360 | 0 | 1 |
| Optional Task 5 | 287 | 5 | 40 | 30 | 30 / 120 | 15,360 | 0 | 1 |

Protected main plus ordinary hard-concrete work is 15,300,000 model-loop
optimizer steps. The protected 615,424 exact evaluations comprise 330,240
main discovery evaluations, 15,360 ordinary-restart evaluations, 7,680 local
evaluations, and 262,144 exact calibration masks. Endpoint/frontier and all 12
packing sensitivity reductions reuse those sealed ledgers and never trigger
rediscovery.

Protected teacher/model-basis ledgers per discovery method are exactly:

- Task 1 direct teachers 45 and eligible students 270;
- Task 2 direct teachers 10 and eligible students 40;
- alternate-architecture eligible students 160;
- alternate-basis direct teachers 40 and eligible students 80.

The exact total is 645 per method. Direct-teacher discovery depends only on its
teacher branch and does not wait for student training. Independent method,
task, seed, and optional branches remain concurrency-safe. Reducers wait on the
appropriate sealed ledgers.

## Resources, storage, and Stage 14 launch gate

The protected lower/central/conservative projections remain the approved v3
values: 162.593 / 740.964 / 2,847.230 accelerator device-hours; 12.759 /
25.249 / 80.343 standalone CPU-core-hours; 2 / 5 / 12 hours serial or weakly
parallel CPU critical path; and 2.484 / 8.057 / 31.523 hours
efficiency-adjusted planning wall time at the dossier's illustrative maximum
useful concurrency. Host CPU support is separately `device-hours * h_support`,
where Stage 14 must measure `h_support`.

Protected expected compact retention is 6.334 / 14.658 / 41.272 GiB; staging
and retry worst case is 39.828 / 83.750 / 203.731 GiB. Requested safety quotas
are 8/64, 32/128, and 64/256 GiB persistent/scratch. Each optional task adds
the separately frozen lower/central/conservative projection and safety quota
from the v3 resource record.

These figures are planning branches, not verified availability. Stage 14 must
bind actual machine counts, hardware classes, qualified CPU/CUDA/MPS rates,
host support cores, RAM/VRAM, permitted intervals, queue/preemption efficiency,
scratch/persistent quotas, and merge/export rates. It must integrate the school
Mac branch over measured per-class intervals; unqualified MPS contributes zero
MPS capacity. If no verified dependency-closed configuration completes the
protected core within `H_science = max(0, H_total - 12 hours)`, launch blocks
pending a prospective amendment. Optional tasks cannot be used to make launch
appear feasible.

## Synthetic complete report

The deterministic fixture covers all five tasks, all protected and optional
tiers, phases, seeds, hard/soft conditions, architectures, bases, discovery
methods, both endpoints, all four calibration layers, and all six Fourier
conditions. It includes eligible and failed attempts, insufficient students,
teacher/phase unavailability, budget censoring, search failure, zero and
censored packing, nonfinite/corrupt/duplicate/stale/conflicting rejection,
partial Tier 2/3 completion, and Tier 1 incompleteness. Fourier comparison sets
exercise winning, tying, losing, failing, and incomplete outcomes while keeping
all controls.

The report contains 17 fixed table/figure-data/tidy surfaces. It resolves all
six claim categories only to synthetic `met`, `not_met`, `indeterminate`, or
`unavailable` states, emits no scientific conclusion, and requires no manual
editing. Its file SHA-256 is
`d66b652f27cab03e3b90e1be0bf0f3557b9fa6244f96413c27c2bd54ad74a096`.

## Downstream interfaces

Austin 6 receives the campaign root, job-array specification and expansion
seal, materialization command, resource projection, optional increment hashes,
P3 dependency identities, P4 retention/export rules, and the explicit Stage 14
binding/launch-block contract.

Alex 6 receives the immutable approval binding, frozen protocol and production
profiles, analysis/report plan, excluded-evidence register, deterministic
synthetic fixture/report generator, complete report topology, and read-only
Stage 13 validator. Any scientific or resource change must be a prospective,
hash-changing amendment; no consumer may mutate these records in place.

## Barrier status

```text
ALEX_05_STAGE13_STATUS=COMPLETE_AT_BARRIER_2
SCIENTIFIC_DATA=NO
DEFINITIVE_EXECUTION_STARTED=NO
RD_001_THROUGH_RD_012=RESOLVED
RD_013=PENDING_STAGE14
RD_014_STAGE13_PORTION=RESOLVED
STAGE14_STARTED=NO
STAGE15_STARTED=NO
```
