# Alex 1 — Stage 11 post-red-team design resolution

## Paste this entire document into one fresh Chat-mode task

Repository: `AK-314/circuit-families-distillation`

Local clone convention: `~/Projects/circuit-families-distillation`

Required exact base:

```text
52b8f602614cb2b830ecc31f9c0200cbdcb4462e
```

Required branch:

```text
feat/stage-11-red-team-design-resolution
```

## Mission

Convert the completed scientific red-team into one coherent candidate design
interface for the remaining implementation. This is Stage 11 only. Do not
implement the new discovery method, basis machinery, cluster runner, or
definitive experiment in this task.

The output must tell later Stage 12 packages exactly what scientific interfaces
they are implementing while leaving unsupported numeric production values open
for the Stage 13 freeze.

Austin simultaneously owns the policy-neutral task/teacher foundation from the
same base. Do not wait for Austin unless an actual interface collision appears.

## Authorities — read completely before acting

1. `docs/distillation_followup/post_red_team_protocol_amendment.md`
2. `docs/distillation_followup/distillation_implementation_post_red_team.md`
3. `docs/distillation_followup/red_team/red_team_resolution_matrix.md`
4. `followup/configs/post_red_team_open_decisions_v1.json`
5. `docs/distillation_followup/stage9_training_backend_benchmark.md`
6. `docs/distillation_followup/stage10_discovery_compute_benchmark.md`
7. `docs/distillation_followup/handoffs/post_red_team/handoff_sequence.md`

The original Stage 2 protocol/master and UD register are immutable historical
authorities. Do not edit them. Record Stage 11 resolutions additively.

## Scientific boundary

Permitted evidence:

- predecessor results already disclosed;
- red-team arguments and responses;
- existing Stage 1–10 code and technical benchmarks;
- task algebra and architecture definitions;
- synthetic, analytic, or excluded technical evidence.

Prohibited evidence:

- newly generated teacher/student comparative outcomes;
- centred-logit circuit effects by phase or condition;
- packing outcomes from candidate production cells;
- Fourier interchange outcomes from candidate scientific pairs;
- apparent result direction used to select any design choice.

No definitive teacher training, student training, circuit discovery, endpoint
analysis, or Fourier scientific execution is authorized.

## Chat protocol

- Stay in Chat mode for this entire handoff. Never switch to Work mode inside
  the task.
- Complete Parts A–F in order.
- Each operational response contains exactly one fenced terminal block.
- Before the block, explain in at most a short paragraph what it will change or
  inspect and what diagnostics it prints.
- Alex pastes complete stdout/stderr back before the next block.
- A part may require several one-block turns. Never declare a part complete
  merely because one command succeeded.
- Do not provide a prose-only completion message when the next block is ready.
- Use focused validation while editing. Run the Stage 11 suite and one relevant
  compatibility gate at the end; do not repeatedly burn time on the full
  historical suite.
- Do not touch or stage user-owned untracked presentation/DOCX files.
- End every response with:

```text
HANDOFF=ALEX_01_STAGE11
COMPLETED_PARTS=<...>
NEXT_PART=<...>
BASE=52b8f602614cb2b830ecc31f9c0200cbdcb4462e
HEAD=<exact current SHA>
WAITING_FOR=<NONE or exact blocker>
SCIENTIFIC_DATA=NO
```

## Required deliverables

Create a small, versioned Stage 11 package, preferably:

- `docs/distillation_followup/stage11_post_red_team_design_resolution.md`;
- `followup/configs/stage11_post_red_team_design_candidates_v1.json`;
- `followup/manifests/stage11_red_team_resolution_v1.json`;
- a validate-only Stage 11 command;
- focused/adversarial tests.

Use repository conventions and existing validation utilities. If a better
repository-consistent path exists, explain it before using it. Do not create a
second schema system.

The package must distinguish:

- **accepted direction:** fixed by the red-team response;
- **candidate interface:** enough for Stage 12 implementation;
- **production value still open:** resolved only in Stage 13;
- **gated extension:** not required for protected Stage 15 completion.

## Part A — Exact-base guard and authority audit

The first block is read-only. It must print and verify:

- repository root and remote URL;
- current branch, HEAD, `main`, and `origin/main`;
- exact base equality with `52b8f602...`;
- clean tracked state plus separately listed untracked files;
- existence and hashes of all authorities;
- PR #15 merged state and head/merge SHAs if GitHub is available;
- no Stage 11 implementation branch or production artifact collision;
- no Stage 15 scientific process/output;
- relevant existing validators and test namespaces.

After Alex returns the output, diagnose it. A second block may create the branch
only if the exact-base and cleanliness guards pass.

**Part A passes when:** the branch is created from the exact base, tracked state
is clean, user-owned untracked files are preserved, and scientific execution is
absent.

## Part B — Coverage audit: red-team findings to design obligations

Build a deterministic crosswalk covering every row of
`red_team_resolution_matrix.md` and every `RD-001` through `RD-014`.

For each item record:

- disposition;
- scientific reason;
- required implementation package;
- required freeze stage;
- planned analysis consequence;
- permitted claim;
- prohibited claim;
- whether failure/unavailability remains reportable;
- whether the item blocks Stage 15.

Adversarial tests must reject:

- missing red-team rows;
- missing or duplicate RD identifiers;
- an accepted item with no implementation consequence;
- a rejected claim silently reintroduced elsewhere;
- a production-blocking decision marked resolved without a resolution record;
- mutation of the historical Stage 2 register.

**Part B passes when:** coverage is complete and machine-checkable; no design
choice has yet been selected from new results.

## Part C — Population, task, architecture, and basis candidates

Define candidate interfaces for:

### Population and phases

- mod-113 addition as the protected primary task;
- construction of additional teacher seeds toward approximately 15 total;
- training-metric-only phase selection;
- matched-teacher availability and unavailable-cell semantics;
- teacher seed as the sole population unit.

Do not fabricate checkpoint availability. The exact production roster remains
open until teachers are constructed under the frozen rule.

### Task breadth

- Task 1: full mod-113 addition study;
- Task 2: reduced modular-multiplication replication;
- Task 3: capacity-contingent modular-polynomial replication whose exact formula
  and rationale remain prospectively resolved, never chosen for favorable
  training or circuit results;
- reduced-task completeness and reporting boundaries;
- no substitution of modular subtraction as fake breadth.

### Architecture panel

- canonical predecessor-matched architecture;
- a systematic candidate panel of up to five student architecture families;
- balanced sparse assignment rather than a full factorial;
- parameter, searchable-component, and component-type accounting;
- explicit prohibition on interpreting a heterogeneous panel as an isolated
  causal effect of depth or width.

### Component bases

- canonical heads plus individual MLP neurons;
- pre-output-projection attention coordinates;
- prospectively seeded balanced neuron blocks;
- limited fixed orthogonal rotations;
- parameter-weighted and type-stratified summaries;
- claims limited to tested bases/partitions.

Specify identities, required metadata, and implementation interfaces. Leave
unjustified numeric roster sizes, block counts, and rotations open for Stage
13.

**Part C passes when:** later task/teacher, architecture, and basis packages can
implement one compatible interface without inferring scientific policy.

## Part D — Endpoint, discovery, null, causal, and resource candidates

Define:

### Endpoints and fidelity

- Endpoint 1 as primary and always defined through the intact mask;
- Endpoint 2 as key secondary unless preproduction validation prospectively
  earns co-primary status;
- one primary centred-logit threshold plus a fidelity frontier;
- ledger reuse rather than duplicate searches for reducer-only settings;
- procedure-relative terminology and censoring/failure visibility.

### Discovery and packing calibration

- inherited discrete greedy/diversity family;
- one genuinely independent continuous/stochastic sparse-mask family;
- separate method-native budgets and common exact allowance;
- combinatorial size/type-matched floor;
- ordinary-restart baseline;
- local fidelity-retaining perturbation null;
- exact/near-exact feasible-region calibration on a tractable model.

### Fourier key secondary

- registered and executed regardless of favorable direction;
- pair-selection inputs that exclude interchange outcomes;
- alignment/intervention/capacity contract;
- wrong-mode, shuffled, mismatched-input, equal-norm random, and unaligned
  patching controls;
- shared-abstraction claim only if every matched control is outperformed;
- no uniqueness claim.

### Tiering and resource contingencies

- protected Tier 1, protected Tier 2 minimum, and optional Tier 3;
- fixed reverse-priority workload shedding;
- Symbolica available branch;
- Symbolica unavailable / school-Mac primary branch;
- Symbolica incomplete / school-Mac recovery branch;
- school-Mac ordering: primary recovery, reproduction, Task 2, then Task 3 only
  after an operational completeness gate;
- post-submission bounded reserve menu, not an open-ended rebuttal redesign.

The literal full factorial must be rejected explicitly.

**Part D passes when:** later discovery, null, Fourier, orchestration, and
analysis packages have unambiguous interfaces and interpretation limits.

## Part E — Records, validation, and synthetic report skeleton

Implement the Stage 11 records and validator. The validator must prove:

- full red-team/RD coverage;
- historical files unchanged;
- every production-blocking item assigned to Stage 13/14 resolution;
- no candidate technical value represented as frozen production policy;
- no full-factorial requirement;
- hard/soft separation;
- teacher-seed population hierarchy;
- Endpoint 1/Endpoint 2 status;
- required method/null/Fourier/basis/architecture/task interfaces;
- resource branches and fixed priority order;
- scientific execution remains false;
- no private path or large/binary scientific artifact.

Create a compact synthetic report skeleton showing where failure, unavailable
tasks, incomplete cells, method dependence, basis dependence, and null Fourier
results would appear. It must contain no newly generated scientific value.

Run focused/adversarial tests and the historical Stage 2 validator.

**Part E passes when:** records round-trip, adversarial mutations reject with
specific diagnostics, the complete planned interface is represented, and the
historical freeze still validates.

## Part F — Commit, exact-SHA double-check, PR, and Barrier 1 report

Before committing:

- inspect the complete changed surface;
- confirm only Stage 11 files plus necessary navigation are changed;
- run focused/adversarial tests;
- run relevant Stage 4/5D/6D compatibility checks where interfaces touch;
- run Ruff on changed Python;
- run diff hygiene, private-path, secret, large-file, and Stage 12/15 absence
  checks;
- confirm tracked cleanliness after commit.

Create coherent commits without amend/force-push. Push the branch and open a PR
against `main`. Perform one fresh-checkout internal double-check at the final
exact SHA. Repair only genuine findings in descendant commits and repeat the
exact-SHA check.

Do not merge from inside this handoff unless the master task explicitly
authorizes it after reviewing the final report.

The final report must contain:

- base, branch, final exact SHA, parent, and PR;
- exact changed files;
- red-team and RD coverage counts;
- candidate versus still-open decisions;
- validation totals;
- internal findings and repairs;
- confirmation of no scientific execution;
- a concise Barrier 1 interface summary for Austin;
- explicit stop before Alex 2 or Stage 15.

Final status:

```text
ALEX_01_STAGE11_STATUS=COMPLETE_AT_BARRIER_1
SCIENTIFIC_DATA=NO
STAGE12_RECOVERY_STARTED=NO
STAGE15_STARTED=NO
```

## Prohibited shortcuts

- Do not treat the prose amendment as sufficient without machine-readable
  coverage and adversarial validation.
- Do not freeze 0.99, architecture counts, budgets, null draws, task formula,
  block sizes, rotations, student counts, or concurrency merely because they
  appeared as working proposals.
- Do not mutate the historical UD register to make decisions look resolved.
- Do not implement Alex 2–6 from this task.
- Do not run private predecessor checkpoints or any definitive endpoint.
