# Alex 5 — Stage 13 protocol, analysis, and production-manifest freeze

## Paste this entire document into one fresh Chat-mode task

Repository: `AK-314/circuit-families-distillation`

Local clone convention: `~/Projects/circuit-families-distillation`

Required integrated Stage 12 floor:

```text
b843323122207c472f3a19edd502c01b266b991b
```

Required accepted implementation heads:

```text
Stage 11 design interface:       6e027a8c22e5228dadad8707f3a262e78028f855
Stage 12-P1 task/teacher:        0ba93daece3328d9bc2aa5ad2296f200a92f26fb
Stage 12-R1 discovery:           b11c32aa0ef2328a86f41096f188a7a055352fd8
Stage 12-P2 architectures:       c4065c2977f2d4e0cd09a54014f60f993f08aceb
Stage 12-R2 bases:               21d80d72376f004656d827069fc53bf55864ce61
Stage 12-P3 campaign DAG:        955d357609f8916797d2ea929f924e8f120e2334
Stage 12-P4 storage/export:      464904c19c3f4bafdd0fc424aa7be4fb3350a831
Stage 12-R3 packing/calibration: ed4d9f3c8dc334a8dd80f8439dbd75826fd75e0f
Stage 12-P5 Fourier runner:      528ee302b5659f7848d098d27bb60d1d26c397eb
```

Required branch:

```text
feat/stage-13-protocol-manifest-freeze
```

Create the branch from current `origin/main` after this handoff is merged. Part
A must prove that the integrated floor and every accepted head above are
ancestors of `origin/main`. Record the then-current `origin/main` SHA as the
implementation base; do not assume the documentation merge SHA in advance.

## Mission

Convert the accepted post-red-team design and all Stage 12 technical machinery
into one prospective, machine-readable, immutable production plan before any
definitive outcome is generated. Stage 13 must:

1. create an evidence-bounded decision dossier for RD-001 through RD-012 and
   the Stage 13 portion of RD-014;
2. obtain Alex's explicit approval for every genuinely underdetermined
   scientific or resource-contingent choice;
3. freeze the teacher/task/phase, student, training, basis, fidelity, endpoint,
   discovery, null, calibration, analysis, Fourier, and tiering protocols;
4. define the exact production profiles and complete declarative Stage 15 DAG;
5. bind every job to canonical identities, inputs, dependencies, seeds,
   budgets, resources, retries, outputs, retention, and terminal states;
6. generate the complete planned report from synthetic records, including
   failures, unavailable cells, censoring, nulls, and unfavorable outcomes;
7. seal configs, schemas, manifests, exclusions, report topology, and hashes;
8. stop before backend qualification, registered execution, or Stage 15.

This is the scientific freeze, not another technical implementation expansion.
Reuse the merged Stage 12 interfaces. Add integration, resolution, manifest,
analysis-plan, and validation code only where needed to make the plan exact and
machine-checkable.

## Authorities — read completely before acting

1. `docs/distillation_followup/post_red_team_protocol_amendment.md`
2. `docs/distillation_followup/stage11_post_red_team_design_resolution.md`
3. `followup/configs/stage11_post_red_team_design_candidates_v1.json`
4. `followup/manifests/stage11_red_team_resolution_v1.json`
5. `followup/configs/post_red_team_open_decisions_v1.json`
6. `docs/distillation_followup/distillation_experimental_protocol_draft.md`
7. `docs/distillation_followup/distillation_implementation_post_red_team.md`
8. `docs/distillation_followup/distillation_implementation_master.md`
9. `docs/distillation_followup/red_team/red_team_resolution_matrix.md`
10. all merged Stage 11 and Stage 12 handoffs, code, schemas, validators, tests,
    reuse audits, technical profiles, and exact-SHA reports;
11. Stage 2 scientific skeleton, unresolved-decision register, and
    method-development firewall;
12. Stage 3 canonical teacher registry and its unavailable-cell accounting;
13. Stage 4 identities, schemas, hashes, seeds, paths, and consumer crosswalk;
14. Stage 5A–D fidelity, trainer/cache, orchestration, and hierarchical-output
    interfaces;
15. Stage 6A–E exact ledgers, hard/soft eligibility, discovery adapters,
    budgets, and endpoint contracts;
16. Stage 7 lifecycle, reproduction, inventory, and registered-fixture
    boundaries;
17. Stage 8–10 technical determinism, backend, and Fourier diagnostics;
18. `docs/distillation_followup/handoffs/post_red_team/handoff_sequence.md`;
19. `docs/distillation_followup/handoffs/post_red_team/shared_stage_15_symbolica_campaign_skeleton.md`.

Historical records are immutable evidence. Resolve decisions using additive
Stage 13 records; do not rewrite a prior register to make it look as if choices
were always settled.

## Evidence firewall and decision rule

Permitted evidence:

- the accepted red-team design and explicit project aims;
- Phase I facts already admitted by the protocol, used only as stated context;
- Stage 9/10 technical benchmarks and Stage 12 synthetic/excluded calibration;
- interface capability, correctness, runtime, memory, storage, determinism,
  and feasibility measurements;
- the actual announced resource envelope, if documented before production;
- conservative resource-contingent branches that do not alter Tier 1 estimands.

Prohibited evidence:

- any comparative Stage 15 or candidate definitive endpoint;
- exploratory runs on registered checkpoints performed to choose a favorable
  threshold, budget, basis, method, phase rule, pair, or analysis;
- unpublished directional results hidden in local files or chat summaries;
- synthetic effect direction used as if it were scientific evidence;
- selecting a value because it strengthens a phase or distillation contrast.

For every RD decision, record candidate set, admissible evidence, excluded
evidence, rationale, exact resolution, owner, approval, downstream consumers,
and amendment rule. When authorities and feasibility do not determine one
choice, present a compact recommendation with alternatives and tradeoffs to
Alex and wait for explicit approval. Do not fabricate consent or freeze a
scientific value merely to keep the chat moving.

## Scientific and operational boundary

Permitted:

- machine-readable configs, schemas, manifests, hashes, validators, planners,
  report specifications, and claim-resolution rules;
- deterministic synthetic records spanning every planned terminal state;
- exact job-count, compute, memory, storage, concurrency, and export estimates;
- resource-contingent manifests whose scientific inclusion rules are fixed;
- temporary materialization of the complete declarative DAG and report;
- validation of every merged Stage 12 interface without registered artifacts.

Prohibited:

- training or evaluating a real teacher or student;
- running discovery, packing, calibration, or Fourier interchange on a
  registered checkpoint or dense-output cache;
- beginning CPU/CUDA production qualification or treating MPS evidence as
  production qualification;
- inventing a Symbolica grant, queue, path, credential, container digest, or
  export destination that has not been supplied and verified;
- changing protected estimands according to expected or observed direction;
- leaving a production-blocking choice as an implicit code default;
- starting Stage 14 or Stage 15.

Every generated validation result and synthetic report must state
`scientific_data=false` and `production_eligible=false`. Frozen production
configs are plans, not scientific results; they must state
`definitive_execution_started=false`.

## Chat protocol

- Stay in Chat mode for the entire handoff; do not mix Chat and Work modes.
- Complete Parts A–H in order. Use additional one-block turns only for a real
  failure, a necessary diagnostic branch, or the mandatory human decision gate.
- Every operational response contains exactly one fenced terminal block.
- Keep blocks short enough to render reliably and print useful diagnostics.
- Briefly state what the block changes or inspects.
- Alex returns complete stdout/stderr before the next block.
- Never emit only a status footer when the next block is available.
- Never claim to await output from a block that was not supplied.
- Use the repository virtual environment when present; do not silently switch
  to an incompatible system interpreter.
- In zsh snippets, do not use reserved names such as `status`.
- Use focused checks while implementing and one broad exact-SHA double-check at
  the end. Do not replay every historical review ritual.
- Preserve unrelated tracked changes and user-owned untracked files.
- End every response with:

```text
HANDOFF=ALEX_05_STAGE13
COMPLETED_PARTS=<...>
NEXT_PART=<...>
BASE=<exact implementation base recorded in Part A>
HEAD=<exact current SHA>
WAITING_FOR=<NONE or exact blocker>
SCIENTIFIC_DATA=NO
```

## Required Stage 13 artifacts

Names may be adjusted only to avoid a proven repository collision, but the
following logical artifacts are mandatory:

1. **Decision resolution record** — additive resolution for RD-001 through
   RD-012 and the Stage 13 portion of RD-014, with RD-013 explicitly retained
   for Stage 14.
2. **Frozen scientific protocol** — exact task, population, phase, student,
   training, eligibility, basis, fidelity, endpoint, discovery, null,
   calibration, analysis, Fourier, and tier definitions.
3. **Production profile set** — exact numeric and algorithm/version settings,
   with no hidden defaults.
4. **Analysis and report plan** — estimands, units, contrasts, missing/failure
   rules, multiplicity/descriptive rules, tables, figures, diagnostics, and
   outcome-category mapping.
5. **Campaign root manifest** — authorities, inputs, config hashes, job counts,
   dependency closure, tier totals, resource projections, and child-manifest
   hashes.
6. **Complete declarative job manifest** — every logical job or a closed,
   deterministic, fully enumerable array specification whose expansion hash is
   sealed and independently reproduced.
7. **Excluded-evidence and excluded-output register** — every development,
   synthetic, red-team, benchmark, or private artifact forbidden from
   definitive inference.
8. **Synthetic complete-report fixture** — all planned report surfaces and
   terminal states without scientific conclusions.
9. **Stage 13 validator and adversarial suite** — cwd-independent, read-only in
   validate-only mode, deterministic, and closed to unknown fields.

Do not commit a huge verbose per-job file merely to claim completeness. Use the
P3/P4 canonical compact/declarative machinery. If a fully expanded manifest is
too large for ordinary Git, commit the closed generator inputs, schema,
canonical root/child hashes, counts, and deterministic materialization command;
seal the exact expanded artifact for Stage 14 staging. The same inputs must
reproduce byte-identical expansion in a fresh checkout.

## Part A — Exact base, complete dependency inventory, and clean branch

The first block is read-only. It must print and verify:

- repository root, remote, branch, HEAD, local `main`, and `origin/main`;
- ancestry of `b843323...` and every accepted implementation head listed at
  the top of this handoff;
- merged state and exact head for the associated Stage 11/12 PRs;
- physical presence and importability of every Stage 12 exported interface;
- open RD-001 through RD-014 register state;
- Stage 3's 15 planned teacher-phase cells, including the two unavailable
  cells, without treating that predecessor roster as the Stage 13 answer;
- tracked cleanliness and separately listed untracked files;
- absence of Stage 13 freeze artifacts and active Stage 14/15 work;
- no registered/private-artifact access or active scientific process;
- current documented resource facts versus still-unknown provider details.

After diagnosing that output, a second block may create the required branch.

Build one cross-lane compatibility inventory mapping every Stage 13 requirement
to its canonical producer, serializer, consumer, and test. Any duplicate or
incompatible identity, seed, mask, ledger, failure, lifecycle, or manifest
contract is blocking and must be repaired before freezing—not papered over by
an adapter with altered semantics.

**Part A passes when:** all accepted Stage 12 heads are integrated, the tracked
state is clean, the compatibility inventory closes, and no scientific/private
artifact has been accessed.

## Part B — Decision dossier and mandatory Alex approval gate

Generate a deterministic, human-readable and machine-readable dossier for
RD-001 through RD-012 and RD-014. For each decision include:

- exact question and downstream consequences;
- Stage 11 candidate and red-team rationale;
- Stage 12 capability and technical evidence;
- viable exact options and invalid options;
- compute/storage consequences by protected tier;
- recommended resolution and why it is not outcome-driven;
- uncertainty, limitation, and amendment consequence;
- whether Alex must choose or the authority already determines the answer.

The dossier must explicitly reconcile:

- expanded teacher population, Tasks 1–3, and phase availability;
- canonical versus alternate architecture allocation;
- hard/soft separation, replication target, attempt cap, and no-imputation
  failure accounting;
- basis/granularity/orientation assignments and weighted denominators;
- centred-logit primary threshold/frontier and denominator guard;
- Endpoint 1 primary status and the prospectively justified status of packing;
- two discovery families, native budgets, common exact allowance, restarts,
  backend determinism, and within-method interpretation;
- all four packing/calibration layers and their minimum coverage;
- hierarchical inference, raw teacher-seed reporting, and missing cells;
- Fourier pair/location/alignment/capacity/outcome/control/success rules;
- Tier 1, protected Tier 2 minimum, Tier 3, shedding order, school-Mac branch,
  and four-day cluster envelope;
- Stage 13 versus Stage 14 ownership of orchestration and resource facts.

Before writing any frozen production value, present Alex with one compact
decision table containing all genuinely underdetermined choices. Provide a
single recommended package plus clearly labeled alternatives and consequences.
Wait for Alex's explicit approval or amendments. This is one meaningful human
gate, not a request for approval after every file.

Record the approved decision package verbatim with date, authority identity,
and dossier hash. If Alex changes an accepted Stage 11 direction, record the
change prospectively with its rationale; do not silently rewrite history.

**Part B passes when:** Alex has explicitly approved one complete package, all
choices are evidence-bounded, and the approval predates the production freeze.

## Part C — Freeze scientific design and production profiles

Translate the approved package into closed, versioned configs and schemas.
Resolve exactly:

### Population, tasks, phases, and students

- teacher task/seed roster, construction/training rule, phase-selection rule,
  unavailable-cell handling, and matched-primary eligibility;
- Task 1 core and the reduced Task 2/3 external-validity protocols;
- architecture roster, initialization roster, parameter/component accounting,
  and sparse balanced assignment;
- hard and soft targets, losses, optimizer/schedule/stopping rules, eligibility
  tolerances, argmax rule, eligible target, attempt caps, replacement rule,
  terminal failures, and minimum reportable cell;
- teacher-seed as population unit; student initialization as conditional
  realization sensitivity; hard and soft never pooled.

### Bases, fidelity, endpoints, discovery, nulls, and calibration

- canonical component basis and predeclared re-granulation/orientation panel;
- intervention semantics, assignment seeds, parameter-weighted and
  type-stratified summaries;
- centred-logit formula, float/accumulation policy, gauge, denominator guard,
  primary threshold, prospective frontier, and ledger-reuse rule;
- Endpoint 1 definition including intact-model proportion `1.0`, procedure
  relativity, failed-as-defined behavior, and censoring;
- Endpoint 2 status, component cap, overlap metric/cutoff, packing solver,
  lower-bound language, failure/censoring, and claim boundary;
- exact discovery-method versions, method-native budgets, common exact
  allowance, restart/termination/failure rules, seeds, and cross-method
  interpretation limitation;
- combinatorial, ordinary-restart, local-perturbation, and tractable
  feasible-region calibration definitions, budgets, draws, certificates,
  minimum coverage, and transfer limitations.

Reject any config with an unresolved field, undocumented default, unsupported
enum, duplicate identity, noncanonical ordering, method-budget unit confusion,
or outcome-dependent selection. Preserve raw quantities required for later
sensitivity analysis even when only one value is primary.

**Part C passes when:** RD-001 through RD-009 have additive, exact resolutions;
every setting is machine-readable and schema-validated; and no definitive job
has run.

## Part D — Freeze analysis, Fourier, tiering, and claim rules

Freeze the complete analysis and interpretation contract:

- primary teacher-seed paired phase contrasts and estimand populations;
- direct-teacher versus student comparisons where scientifically defined;
- student-cell summaries and realization-dispersion summaries;
- architecture, basis, component type, and discovery method as conditional
  repeated factors rather than extra population replicates;
- hard/soft separation and unconditional attempt/failure accounting;
- minimum complete/matched units, unavailable and unresolved cells, no
  imputation, no opportunistic replacement, and partial-completion language;
- population model, uncertainty intervals/tests if used, descriptive raw seed
  reporting, multiplicity policy, and sensitivity hierarchy;
- required tables, figures, inventories, failure reports, resource reports,
  and machine-readable tidy outputs;
- the six frozen outcome categories without turning them into directional
  predictions;
- permissible claims for Endpoint 1, packing lower bounds, null calibration,
  tractable calibration, basis sensitivity, and method dependence.

Freeze Fourier interchange using P5:

- outcome-independent pair roster and selection evidence;
- representation, mode rule, intervention location, alignment and fitting-data
  boundary;
- operational information-capacity accounting including side information;
- outcome, trial/input/seed roster, aggregation, comparison, and success rule;
- aligned condition plus all five capacity-matched controls;
- complete comparison-set/failure rule;
- shared-abstraction claim only if aligned outperforms every control under the
  frozen rule; uniqueness claims prohibited.

Freeze scope and priority:

- exact protected Tier 1 matrix;
- exact protected Tier 2 minimum;
- optional Tier 3 ordered list;
- balance and reportability rules;
- reverse shedding order and triggers based only on operational completeness;
- school-Mac recovery/extra-task branch without reliance on its availability;
- rules for unused or additional cluster capacity that cannot alter primary
  inclusion or interpretation.

RD-010, RD-011, and RD-012 must close here. RD-013 remains explicitly open for
Stage 14 hardware qualification. RD-014 receives a Stage 13 scientific-DAG,
priority, retry, retention, gate, and export-policy resolution but remains
pending only for verified Stage 14 provider/container/path/quota facts.

**Part D passes when:** every possible terminal dataset maps deterministically
to a prespecified report and claim category, and resource changes can alter
only which preordered optional work runs—not the scientific rules.

## Part E — Complete declarative Stage 15 DAG and sealed manifests

Materialize the full Stage 15 plan through the merged P1–P5/R1–R3 interfaces.
Every planned logical job or closed job-array member must have:

- canonical campaign, task, teacher, phase, distillation, architecture,
  initialization, basis, method, threshold/frontier, null/calibration,
  Fourier, and attempt identities as applicable;
- domain-separated deterministic seeds;
- exact dependency identities and prerequisite artifact hashes;
- frozen config/interface versions and scientific profile hash;
- method-native and common exact budgets in separate units;
- resource class plus conservative runtime, memory, scratch, persistent, and
  transfer estimates;
- tier, protected status, priority, concurrency group, and shedding behavior;
- output root, expected canonical/compact records, manifest, merge, retention,
  recomputation, and export rules;
- retry cap, retryability, terminal failure/unavailable/censored state, and
  no-imputation consequences.

The dependency graph must permit safe concurrency: direct-teacher work need not
wait for students; independent task/seed branches may run together; reducers
that reuse one exact ledger must not duplicate discovery jobs. It must include
qualification prerequisites and the three human gates but must not embed an
unverified provider queue, path, credential, or container digest.

Generate and validate:

- exact counts by family, task, phase, teacher seed, student condition,
  architecture, basis, method, endpoint, null/calibration, Fourier condition,
  and tier;
- complete dependency closure and acyclicity;
- protected-minimum coverage and balance;
- worst-case attempt/failure counts;
- conservative compute, wall-clock, memory, scratch, persistent, transfer, and
  final-audit-window projections;
- deterministic shard, merge, recomputation, compact-storage, and export plan;
- capacity branches for the intended four-day cluster envelope and interrupted
  school-Mac overflow, without assuming either resource exists.

Independently expand/recompute the manifest twice from the frozen inputs. Counts,
ordered identities, canonical bytes, and hashes must match. A resource envelope
that cannot cover Tier 1 is a visible Stage 14 launch blocker, not permission to
shrink the estimand here.

**Part E passes when:** the complete campaign is finite, enumerable,
dependency-closed, resource-projected, hash-sealed, and contains no implicit
scientific defaults.

## Part F — Generate the complete planned report from synthetic records

Use deterministic synthetic records only. Cover at minimum:

- all planned tasks, phases, teacher seeds, hard/soft conditions,
  architectures, bases, methods, endpoints, and protected tiers;
- exact hard eligibility, soft eligibility, failed attempts, insufficient
  eligible students, teacher unavailability, missing phase cells, budget
  exhaustion, search failure, packing zero/lower-bound/censoring, null and
  tractable calibration outcomes;
- aligned Fourier winning, tying, losing, failing, and incomplete comparison
  sets while preserving every control;
- architecture/basis/method disagreement;
- partial Tier 2/3 completion with intact Tier 1;
- a Tier 1-incomplete terminal campaign;
- nonfinite, corrupted, duplicate, stale, and conflicting records where the
  frozen policy must reject or report them.

Generate every required table, figure-data table, inventory, failure table,
raw teacher-seed summary, student-cell summary, uncertainty/result field, null
diagnostic, Fourier comparison, resource/accounting table, and claim-resolution
category. Visual rendering may remain a later reporting task, but its exact
data contract, labels, denominators, facets, ordering, and missingness must be
proven now.

The synthetic report must never select settings, suppress unfavorable cases,
or emit prose claiming a real effect. Its purpose is to prove report topology
and deterministic handling of every outcome category.

Run the report generator from repository root and an unrelated cwd under at
least two `PYTHONHASHSEED` values. Canonical report bytes/hashes must match.

**Part F passes when:** every frozen estimand and failure path appears in the
planned report without manual editing and no scientific claim is produced.

## Part G — Adversarial freeze audit and integrated validation

Build one read-only Stage 13 validator and adversarial suite. It must reject:

- any open production-blocking RD except RD-013 and the explicitly enumerated
  Stage 14-only RD-014 provider facts;
- changed authority or accepted Stage 12 head;
- missing/unknown config fields or unsealed hashes;
- post-approval decision edits and unrecorded defaults;
- outcome-informed selection or forbidden-evidence references;
- incomplete teacher/student/task/architecture/basis/method/null/Fourier/tier
  rosters;
- hard/soft pooling or wrong hierarchy/population unit;
- Endpoint 1 lacking the intact fallback or mislabeled as global minimum;
- packing lacking lower-bound/procedure language or resource caveat;
- missing Fourier control or unequal capacity contract;
- duplicated discovery for ledger-derived reducer settings;
- seed collisions, identity collisions, dependency cycles, orphan jobs, and
  unreachable expected outputs;
- inconsistent manifest counts, resource totals, hashes, retention, retries,
  terminal states, or report rows;
- private paths, credentials, provider assumptions, registered artifacts,
  development outputs, large files, binaries, archives, checkpoints, or LFS
  pointers in the changed surface;
- any indication that Stage 14/15 or definitive execution started.

Run a bounded integrated suite covering Stage 13 plus direct consumers from
Stage 2–12. Include both discovery families, alternate architecture and basis,
all four packing/calibration layers, all six Fourier conditions, P3 lifecycle,
P4 compact export, hard/soft separation, exact-ledger reuse, and full report
generation. Run Ruff, diff hygiene, schema/manifest validation, unrelated-cwd
execution, and cross-hash-seed determinism.

Do one independent recomputation of decision/config/manifest/report hashes from
canonical source records rather than trusting stored hash fields.

**Part G passes when:** the prospective freeze is closed, internally
consistent, reproducible, and incapable of silently changing scientific scope
or interpretation.

## Part H — Commit, exact-SHA double-check, PR, and stop

Inspect the changed surface. It should contain only Stage 13 additive decision,
protocol, config, schema, manifest/planner, report-plan/generator, validation,
tests, and necessary documentation files. Historical authority records and
Stage 12 implementations must remain unchanged unless a demonstrated blocking
integration defect requires a narrow descendant repair with explicit evidence.

Create coherent commits without amend or force-push. At the final exact SHA,
run one fresh detached-checkout internal double-check covering:

- decision approval identity and evidence firewall;
- all resolved/open RD classifications;
- schema closure and frozen config hashes;
- two independent full manifest expansions and count/hash equality;
- complete synthetic report generation under unrelated cwd/hash seeds;
- Stage 2–12 compatibility surfaces actually consumed by Stage 13;
- adversarial selection, identity, dependency, capacity, missingness, failure,
  storage, and claim-boundary cases;
- Ruff, diff, tracked cleanliness, file sizes, private/secret/binary/archive/
  checkpoint/LFS hygiene;
- explicit proof that no registered artifact or scientific job was accessed.

Classify findings as blocking, nonblocking, or question. Repair blockers only
through descendant commits and repeat against the new exact SHA. Push and open
a PR against `main`. Do not merge inside this handoff without master-task
authorization.

Final report:

- base, branch, parent, final SHA, and PR;
- accepted Stage 11/12 ancestry and compatibility evidence;
- Alex approval record and any changes from Stage 11 working directions;
- exact resolution summary for RD-001 through RD-012;
- exact Stage 13 versus Stage 14 split for RD-013/RD-014;
- frozen config/schema/decision/manifest/report/exclusion paths and hashes;
- complete job counts, tier counts, resource projections, and capacity branches;
- synthetic complete-report hash and coverage;
- validation totals, deterministic recomputation, internal findings, and
  descendant repairs;
- confirmation of no registered/private/scientific execution;
- interfaces exported to Austin 6 and Alex 6;
- explicit stop before Stage 14 qualification and Stage 15 production.

Final status:

```text
ALEX_05_STAGE13_STATUS=COMPLETE_AT_BARRIER_2
SCIENTIFIC_DATA=NO
DEFINITIVE_EXECUTION_STARTED=NO
RD_001_THROUGH_RD_012=RESOLVED
RD_013=PENDING_STAGE14
RD_014_STAGE13_PORTION=RESOLVED
STAGE14_STARTED=NO
STAGE15_STARTED=NO
```

## Prohibited shortcuts

- Do not let a code default become an unreviewed scientific choice.
- Do not use registered comparative outcomes to choose any setting.
- Do not ask Alex to approve dozens of files one by one; use the single Part B
  package gate, then validate mechanically.
- Do not preserve an oversized literal full factorial after the accepted
  sparse-tier design.
- Do not count student initializations, architectures, bases, methods, circuits,
  or thresholds as independent population replicates.
- Do not pool hard and soft students.
- Do not impute unavailable or failed cells.
- Do not call Endpoint 1 the globally minimum circuit size.
- Do not call Endpoint 2 an exact packing number when it is a procedural lower
  bound.
- Do not compare raw method packing counts as perfectly resource-matched.
- Do not omit a Fourier control or claim algorithmic uniqueness.
- Do not duplicate discovery jobs for thresholds/caps/overlaps recoverable from
  sealed ledgers.
- Do not assume Symbolica, school Macs, paths, credentials, GPUs, quotas, or a
  container before Stage 14 verifies them.
- Do not hide an infeasible Tier 1 plan by silently dropping jobs.
- Do not commit large generated outputs, registered artifacts, checkpoints,
  archives, credentials, private paths, or LFS objects.
- Do not begin Stage 14, Stage 15, or definitive scientific execution.
