# Stage 13 decision dossier — approval gate

**Status:** recommended package pending Alex approval  
**Implementation base:** `7976c98cc83a6df098ae0ef8c59b56027a7f4899`  
**Scientific data:** no  
**Definitive execution started:** no

This dossier is additive. It does not rewrite the Stage 2 or Stage 11 records.
Its machine-readable authority is
`followup/decisions/stage13_decision_dossier_v1.json`. The complete rationales,
alternatives, protected-tier consequences, uncertainties, and amendment rules
are retained there.

## Evidence boundary

The recommendation uses the accepted red-team amendment, declared project
aims, admitted Phase I context, Stage 9/10 technical runtime and determinism
evidence, and Stage 12 synthetic interface validation. It does not use a
comparative definitive endpoint, registered-checkpoint exploratory endpoint,
unpublished direction, or synthetic effect direction. The documented 96-hour
cluster envelope is treated only as an intended planning branch, not as a
verified resource grant.

## Compact approval table

| Decision | Recommended Package A | Main alternative and consequence |
|---|---|---|
| RD-001 population/tasks | Fixed Task 1 seeds 0–14, no replacement; metric-only pre/50%/stable phases; primary pre vs stable matched rule. Task 2 is multiplication mod 59; Task 3 is `(x²+xy+y²) mod 59`. | Seeds 0–19 then first 15 complete improves matched count but adds a phase-availability selection rule. |
| RD-002 architectures | Canonical plus depth-2, 8-head, MLP-256, and MLP-1024 variants; four alternates balanced over fixed seeds 0–11. | Two alternates reduce cost but weaken architecture external validity. |
| RD-003 students | Tier 1 targets 3 eligible of 6 attempts, minimum 3; Tier 2 targets 2 of 4. Hard uses exact argmax; soft uses centered-logit MSE ≤ `1e-4` plus exact argmax. | Two of four and `1e-3` soft tolerance is cheaper but weakens dispersion and matching. |
| RD-004 bases | Canonical Tier 1; attention coordinates, one seeded 32-neuron block partition, and two seeded QR rotations in Tier 2; raw, parameter-weighted, and type-stratified summaries. | Fewer basis families fails an accepted red-team repair; more belongs in Tier 3. |
| RD-005 fidelity | Primary `0.99`; frontier `0.90, 0.95, 0.975, 0.99, 0.995`; float64 accumulation; exact-zero denominator guard; sealed-ledger reuse. | Primary `0.95` is less stringent and departs from the prospective draft recommendation. |
| RD-006 endpoints | Endpoint 1 primary; packing key secondary; component cap `0.25`; retained Jaccard ≤ `0.25`; exact maximum-compatible-subset solver. | Cap `0.375` follows a technical fixture but that fixture is explicitly nonbinding. |
| RD-007 discovery | Greedy deletion, 516 native proposals; hard-concrete gates, 4×5000 optimizer steps; common exact allowance 256; within-method interpretation. | Half budgets reduce cost but reduce candidate coverage. |
| RD-008 nulls | 10,000 combinatorial draws; 16 ordinary restarts; 128 local exact proposals around up to four seeds; exact tractable layer; 90% minimum coverage. | Smaller counts are cheaper but noisier and more failure-sensitive. |
| RD-009 tractable calibration | Addition mod 7, one-layer 16-dimensional model, 18 components, exhaustive `2^18 = 262,144` masks and exact certificate. | A 24-component branch-and-bound problem is closer to main scale but risks incomplete certification. |
| RD-010 analysis | Teacher-seed paired mean estimand; median student cell; raw/MAD/range reporting; 10,000 teacher-seed bootstrap interval; exact sign-flip tests with Holm within primary families; minimum 10 matched seeds; no imputation. | A multilevel model adds prior/convergence choices not determined by current evidence. |
| RD-011 Fourier | Up to 60 teacher→canonical-student pairs across seeds, primary phases, and hard/soft; fixed four 2-D modes; final residual location; disjoint-fit Procrustes; 256 trials; all five controls; aligned must beat every control with positive lower interval bounds. | Stable-post-only halves cost but loses the phase comparison. |
| RD-012 tiers | Tier 1 protects the full canonical Task 1 matrix. Tier 2 protects the sparse architecture/basis panels, Task 2, frontier, exact calibration, and Fourier. Tier 3 has a fixed reverse-shedding order. | Making all external validity optional weakens the accepted red-team design. |
| RD-014 Stage 13 portion | Two infrastructure retries, deterministic gzip level 6, two retained recovery generations, three gates, 12-hour audit reserve, verified resumable export. Provider facts remain Stage 14-only. | No retries and a 6-hour reserve are cheaper but less robust. |

RD-013 remains wholly pending for Stage 14 backend qualification. RD-014 remains
pending only for the explicitly enumerated provider, hardware, container,
quota, path, credential, concurrency, and destination facts.

## Approval required

Nothing in this file is a frozen production value until Alex approves a complete
package. The recommended response is:

> I approve `stage13-package-a-conservative-protected/v1` exactly as described
> in the Stage 13 decision dossier at its reported SHA-256.

Alex may instead name Package B or C, or state amendments by decision ID. An
approval is recorded verbatim with its date and the dossier hash before any
production config is created.
